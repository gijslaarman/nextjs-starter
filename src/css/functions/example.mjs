/**
 * Example function
 * @param {string} unit - The unit to be used in the function
 * @returns {string} - The formatted string
 */

export function example(unit) {
  return `I am an example function with a unit: ${unit}`;
}
